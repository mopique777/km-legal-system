# نشر نظام إدارة المحاماة على Railway

## 🚀 خطوات النشر السريعة

### 1. إنشاء حساب على Railway
- اذهب إلى [railway.app](https://railway.app)
- سجل حساب جديد (مجاني)
- تحقق من بريدك الإلكتروني

### 2. ربط مع GitHub
- قم بربط حسابك على GitHub مع Railway
- هذا سيسمح لـ Railway برفع المشروع تلقائياً

### 3. إنشاء مشروع جديد
- اضغط على "New Project"
- اختر "Deploy from GitHub"
- اختر مستودع `km-legal-system`

### 4. إضافة المتغيرات البيئية
في لوحة تحكم Railway، أضف المتغيرات التالية:

```
MONGO_URL=mongodb+srv://username:password@cluster.mongodb.net/km-legal
DB_NAME=km-legal
GOOGLE_DRIVE_CREDENTIALS=your_credentials_json
GOOGLE_GENERATIVE_AI_KEY=your_api_key
```

### 5. النشر التلقائي
- Railway سيقوم بالبناء والنشر تلقائياً
- ستحصل على رابط مباشر للتطبيق

---

## 🐳 النشر المحلي باستخدام Docker

### المتطلبات
- Docker و Docker Compose مثبتة

### الخطوات
```bash
cd /home/ubuntu/km-legal-system

# بناء الصور
docker-compose build

# تشغيل التطبيق
docker-compose up
```

التطبيق سيكون متاحاً على:
- Frontend: http://localhost:3000
- Backend: http://localhost:8000
- MongoDB: localhost:27017

---

## 📋 المتطلبات البيئية

### Backend
- `MONGO_URL`: رابط قاعدة البيانات MongoDB
- `DB_NAME`: اسم قاعدة البيانات
- `GOOGLE_DRIVE_CREDENTIALS`: بيانات اعتماد Google Drive (JSON)
- `GOOGLE_GENERATIVE_AI_KEY`: مفتاح API Google Generative AI

### Frontend
- لا يحتاج متغيرات بيئية إضافية

---

## ✅ التحقق من النشر

بعد النشر على Railway:

1. اذهب إلى رابط التطبيق المعطى
2. تحقق من أن الصفحة الرئيسية تحمل
3. اختبر الملاحة والميزات الأساسية
4. تأكد من اتصال قاعدة البيانات

---

## 🔧 استكشاف الأخطاء

### إذا لم يعمل التطبيق
1. تحقق من السجلات في لوحة تحكم Railway
2. تأكد من صحة متغيرات البيئة
3. تأكد من اتصال MongoDB

### إذا كان التطبيق بطيء
1. تحقق من موارد الخادم
2. قد تحتاج لترقية الخطة على Railway

---

## 📞 الدعم

للمساعدة:
- اتصل بـ Railway Support
- تحقق من السجلات في لوحة التحكم
- راجع وثائق Railway الرسمية

---

**تم إنشاء هذا الملف**: 2026-02-07
**الحالة**: جاهز للنشر
